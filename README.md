# Pomodoro CLI Tool

- This is a basic CLI tool made for macos to start a pomodoro timer made in C++ using Ncurses.
- It has been tested for macos and can be installed with brew.
- To install run the following:
```
brew tap Siam5ell/cli_tools
brew trust Siam5ell/cli_tools
brew install pomo_cli
```
